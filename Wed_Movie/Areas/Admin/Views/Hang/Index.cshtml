@model Wed_Movie.DAO.HangDAO
@{
    ViewData["Title"] = "Thể Loại";
}


<div class="col-sm-2">
    <button id="add_Hang" type="button" class="my-3 p-2 btn btn-primary" data-bs-toggle="modal" data-bs-target="#modal_FormHang">Thêm Hãng Phim</button>
</div>
<table id="table" class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody class="dsHang">
    </tbody>
</table>




<!-- Modal -->
<div class="modal fade" tabindex="-1" id="modal_FormHang" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="formTitel_Hang"></h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <form class="" id="formValid_Hang">
                    <input asp-for="Id" type="hidden" id="Id" value="" />
                    <div class="form-group">
                        <label asp-for="Name" class="control-label"></label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary close" data-bs-dismiss="modal">Đóng</button>
                        <button id="btnSubmit" type="submit" class=" btn btn-primary">Xác Nhận</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    <script>
        $(document).ready(function () {
            GetDsHang();
            $('#modal_FormHang').on('hidden.bs.modal', function (e) {
                $("#Id").val("");
                document.getElementById('formValid_Hang').reset();
            });
        });

        //Lấy danh sách thể loại
        function GetDsHang(search) {
            $.ajax({
                url: '/Admin/Hang/GetDsHang',
                type: 'post',
                data: {
                    search: search,
                },
                success: function (data) {
                    if (data.code == 200) {
                        $("#table tbody").empty();
                        $.each(data.dsHang, function (k, v) {
                            var str = '<tr class="" id="' + v.id + '">';
                            str += "<td>" + v.name + "<td>";
                            str += '<div class="btn-group" role="group">';
                            str += '<button type="button" class="btn btn-warning" name="Update" data-bs-toggle="modal" data-bs-target="#modal_FormHang"><i class="fa fa-compress"></i></button>';
                            str += '<button class="btn btn-xs btn-danger text-end" name="Delete"><i class="fa fa-trash" aria-hidden="true"></i></button>';
                            str += '</div>';
                            str += "</td>";
                            str += "</tr>";

                            $("#table tbody").append(str);
                        });
                    }
                    else if (data.code == 500) {
                        alert(data.msg);
                    }
                }
            });
        };
        //Gọi form nhập thể loại
        $("#add_Hang").click(function () {
            document.getElementById('formTitel_Hang').innerText = "Thêm Hãng Phim";
            $("#modal_FormHang").modal();
        });


        //Xóa Dữ Liệu Thể Loại
        $(document).on(`click`, "button[name=Delete]", function () {
            var idHang = $(this).closest('tr').attr('id');

            $.ajax({
                url: '/Admin/Hang/DeleteHang',
                type: 'post',
                data: {
                    id: idHang,
                },
                success: function (data) {
                    if (data.code == 200) {
                        alert(data.msg);
                        GetDsHang();
                    }
                    else if (data.code == 500) {
                        alert(data.msg);
                    }
                }
            });
        });

        //Lấy Dữ Liệu Thể Loại
        $(document).on(`click`, "button[name=Update]", function () {
            var nameAction = "Update";
            var nameHeader = "Cập Nhật Hãng Phim";
            var idHang = $(this).closest('tr').attr('id');

            $.ajax({
                url: '/Admin/Hang/GetHang',
                type: 'get',
                data: {
                    id: idHang,
                },
                success: function (data) {
                    if (data.code == 200) {
                        console.log(data);
                        $("#Name").val(data.hang.name);
                        $("#Id").val(idHang);
                        document.getElementById('formTitel_Hang').innerText = nameHeader;
                        $("#modal_FormHang").modal();
                    }
                    else if (data.code == 500) {
                        alert(data.msg);
                    }
                }
            });
        });


        //Nghe Sự Kiện
        document.addEventListener('DOMContentLoaded', (event) => {

            //Tìm Kiếm
            document.getElementById("search").addEventListener("submit", function (e) {
                e.preventDefault();
                GetDsHang($("#search").val());
            });
            document.getElementById("search").addEventListener("keyup", function (e) {
                GetDsHang($("#search").val());
            });

            //Thêm Và Cập Thê Loai
            document.getElementById("formValid_Hang").addEventListener("submit", function (e) {
                e.preventDefault(); // Cancel the default action
                var nameHang = $("#Name").val();
                var idHang = $("#Id").val();
                if (idHang.length > 0) {
                    $.ajax({
                        url: '/Admin/Hang/UpdateHang',
                        type: 'post',
                        data: {
                            id: idHang,
                            name: nameHang,
                        },
                        success: function (data) {
                            if (data.code == 200) {
                                alert(data.msg);
                                GetDsHang();
                            }
                            else if (data.code == 500) {
                                alert(data.msg);
                            }
                            $("#Name").val("");
                            $("#Id").val("");
                            $(".close").click();
                        }
                    });
                }
                else if(nameHang.length > 0){
                    $.ajax({
                        url: '/Admin/Hang/AddHang',
                        type: 'post',
                        data: {
                            name: nameHang,
                        },
                        success: function (data) {
                            console.log(data);
                            if (data.code == 200) {
                                alert(data.msg);
                                GetDsHang();
                            }
                            else if (data.code == 500) {
                                alert(data.msg);
                            }
                            $("#Name").val("");
                        }
                    });
                }
            });
        });
    </script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
